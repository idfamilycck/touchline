// 엔진 검증 수치(표시용 단일 소스).
//
// 이 앱의 카운터팩추얼("당신이 지휘했다면 실제 패배가 승리가 됐을 것")은 엔진이
// 현실을 얼마나 재현하느냐에 신뢰가 걸려 있다. 그래서 엔진의 킥오프 승률 모델(분석적
// poisson)을 실제 2026 월드컵 104경기 전량에 무개입(실제 선발 라인업)으로 적용해,
// 실제 결과를 얼마나 맞히는지 측정했다.
//
// 예측 방식: 각 경기의 킥오프 λ(기대 득점)로 poisson 승/무/패 확률을 구하고 승/패 중
// 큰 쪽을 예측으로 삼는다(무승부는 저득점 스포츠 특성상 단일 최빈 결과가 되는 일이
// 드물어, 무승부를 예측에 넣으면 오히려 정확도가 떨어진다 — 실측으로 확인). 이 모델은
// 앱의 실시간 승률 게이지를 구동하는 바로 그 계산이라, 검증 수치와 화면 게이지가 같은
// 근거를 공유한다.
//
// 아래 값은 lib/engine/validation.test.ts가 매 실행마다 재계산해 검증한다(결정론적).
// 엔진을 바꿔 수치가 움직이면 그 테스트가 실패하므로, 화면·기획서에 쓰는 이 숫자가
// 실측과 어긋날 수 없다.

export const ENGINE_VALIDATION = {
  /** 검증에 쓴 실제 경기 수(대회 전량). */
  matches: 104,
  /**
   * 승/무/패 전체 재현율(%). 무작위 기준선 33% 대비.
   *
   * 이력: 58.3 -> 57.7(결승 추가) -> 58.7(엔진 확장) -> 62.5. 마지막 상승은 ELO를
   * eloratings.net/2026 실측(스프레드 1411~2259)으로 갱신한 뒤 λ 계수를 재적합한
   * 결과다(ELO_MULT_COEF 0.35->0.45, LAMBDA_ELASTICITY 1.6->1.4 — constants.ts 참고).
   */
  outcomeRatePct: 62.5,
  /**
   * 승부가 갈린 경기(무승부 제외)에서 이긴 팀 재현율(%). 대표 지표.
   *
   * 80.0 -> 81.3 -> 86.7(65/75). 마지막 상승은 갱신된 ELO 스프레드에 맞춰 λ 계수를
   * 재적합한 것으로, 넓어진 전력차를 λ에 더 정확히 실어 승자 판정이 4경기 더 맞았다.
   * WC 밸런스 게이트(블로아웃 4.6%<6% 등)는 그대로 통과한다.
   */
  decisiveWinRatePct: 86.7,
  /**
   * 팀당 평균 득점 오차(골). 시뮬레이션 평균 스코어 vs 실제.
   *
   * 0.87 -> 0.94 -> 0.93. 엔진 확장으로 한때 늘었던 점 예측 오차가, ELO 재적합
   * (탄성 하향으로 대량 실점이 줄며)으로 다시 소폭 내려갔다. 재현율 개선과 함께
   * 온 개선이라 트레이드오프가 아니다.
   */
  goalMae: 0.93,
} as const;
